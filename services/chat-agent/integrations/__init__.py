# integrations/ — adapters for external project-management tools.
#
# Each adapter implements the PMIntegration interface from base.py.
# Currently: Jira Cloud (jira.py) and GitHub Issues (github.py).
# Step 7 will fill in the write-side methods (create_item / update_item).
